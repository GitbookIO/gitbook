---
"gitbook": patch
---

Sync the API reference responses selector with the "Responses" collapsibles: selecting a status code in the response example dropdown now expands the matching response section, and vice versa.
