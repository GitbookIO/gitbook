---
"gitbook": patch
---

Fix heading anchor links being unreachable on touch devices by adding tap-to-reveal instead of a hover-only, viewport-width-gated reveal. On touch devices, the anchor icon now renders inline after the heading text while retaining its existing desktop placement.
