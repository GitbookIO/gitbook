---
"@gitbook/react-contentkit": patch
"gitbook": patch
---

Expose the current page (`id`, `path`, `title`) to integration block webframes through the client-only webframe state, gated behind the integration's page-context scope, alongside adaptive visitor claims.
